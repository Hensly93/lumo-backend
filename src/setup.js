require("dotenv").config();const pool=require("./db");async function setup(){try{await pool.query(`CREATE TABLE IF NOT EXISTS usuarios(id SERIAL PRIMARY KEY,nombre VARCHAR(100) NOT NULL,email VARCHAR(100) UNIQUE NOT NULL,password VARCHAR(255) NOT NULL,negocio VARCHAR(100) NOT NULL,created_at TIMESTAMP DEFAULT NOW())`);await pool.query(`CREATE TABLE IF NOT EXISTS transacciones(id SERIAL PRIMARY KEY,usuario_id INTEGER REFERENCES usuarios(id),monto DECIMAL(10,2),tipo VARCHAR(50),empleado VARCHAR(100),turno VARCHAR(50),fecha TIMESTAMP DEFAULT NOW())`);console.log("Base de datos lista");process.exit(0)}catch(e){console.error(e);process.exit(1)}}setup();